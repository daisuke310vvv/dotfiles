# dotfiles

daisuke310vvv's dotfiles - 開発環境の設定ファイル群

## 📁 ディレクトリ構成

```
dotfiles/
├── bin/          # インストール・セットアップスクリプト
├── claude/       # Claude Code 設定
│   ├── agents/   # カスタムエージェント
│   ├── commands/ # カスタムコマンド
│   ├── rules/    # プロジェクトルール
│   └── CLAUDE.md # Claude Code メイン設定
├── ghostty/      # Ghostty ターミナル設定
├── nvim/         # Neovim 設定
├── tmux/         # tmux 設定
└── zsh/          # Zsh 設定
```

## 🚀 インストール

```bash
git clone https://github.com/daisuke310vvv/dotfiles.git ~/github.com/daisuke310vvv/dotfiles
cd ~/github.com/daisuke310vvv/dotfiles
./bin/install.sh
```

## 🔧 含まれる設定

| ツール | 説明 |
|--------|------|
| **Zsh** | シェル設定、エイリアス、プロンプト |
| **Neovim** | エディタ設定、プラグイン |
| **tmux** | ターミナルマルチプレクサ設定 |
| **Ghostty** | ターミナルエミュレータ設定 |
| **Claude Code** | AI アシスタント設定 |

## 📝 使い方

### シンボリックリンクの作成

各設定ファイルは `install.sh` によってホームディレクトリにシンボリックリンクが作成されます。

### 個別設定の適用

特定のツールのみ設定したい場合は、手動でシンボリックリンクを作成できます：

```bash
# 例: Neovim設定のみ適用
ln -sf ~/github.com/daisuke310vvv/dotfiles/nvim ~/.config/nvim

# 例: tmux設定のみ適用
ln -sf ~/github.com/daisuke310vvv/dotfiles/tmux/.tmux.conf ~/.tmux.conf
```

## 📄 ライセンス

MIT
